import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  root: {
    flex: 1,
  },
  marginTop: {
    marginTop: 10,
  },
  button: {
    marginBottom: 30,
    marginHorizontal: 20,
  },
});

export default styles;
